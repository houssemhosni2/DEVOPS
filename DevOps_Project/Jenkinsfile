pipeline {
    agent any

    environment {
        // Set Maven and Java Home paths
        MAVEN_HOME = "/opt/apache-maven"
        JAVA_HOME = "/usr/lib/jvm/java-17-openjdk-amd64"
        PATH = "${env.PATH}:${MAVEN_HOME}/bin:${JAVA_HOME}/bin"
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'koussay1',
                url: "https://github.com/zankech/project.git"
            }
        }

        stage('Build') {
            steps {
                dir('DevOps_Project') {
                    sh 'mvn clean package'
                }
            }
        }
    }
}
